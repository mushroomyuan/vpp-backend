apiVersion: v1
kind: ConfigMap
metadata:
  name: simulator-config
data:
  SIMULATOR_HTTP_ADDR: "0.0.0.0:8084"
  RESOURCE_GRPC_ADDR: resource.vpp.svc.cluster.local:5002
  # In-cluster gateway until APISIX upstreams point at kind NodePort.
  GATEWAY_HTTP_ADDR: http://gateway.vpp.svc.cluster.local:8083
