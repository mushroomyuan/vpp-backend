# Dev-only credentials, matching compose.yaml / config/*.yaml.
# Not for any shared or production cluster.
apiVersion: v1
kind: Secret
metadata:
  name: vpp-credentials
  labels:
    app.kubernetes.io/part-of: vpp-backend
type: Opaque
stringData:
  postgres-user: postgres
  postgres-password: postgres123
  casdoor-password: "123"
  simulator-api-key: vpp-dev-simulator-key
