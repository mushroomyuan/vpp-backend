apiVersion: v1
kind: Service
metadata:
  name: telemetry
  labels:
    app.kubernetes.io/name: telemetry
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: telemetry
  ports:
    - name: grpc
      port: 5003
      targetPort: grpc
