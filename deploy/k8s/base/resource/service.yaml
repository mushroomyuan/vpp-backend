apiVersion: v1
kind: Service
metadata:
  name: resource
  labels:
    app.kubernetes.io/name: resource
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: resource
  ports:
    - name: http
      port: 8082
      targetPort: http
    - name: grpc
      port: 5002
      targetPort: grpc
